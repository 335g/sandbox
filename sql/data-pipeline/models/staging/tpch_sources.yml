version: 2

sources:
  - name: tpch
    database: snowflake_sample_data
    schema: tpch_sf1
    tables:
      - name: orders
        columns:
          - names: o_orderkey
            tests:
              - unique
      - name: lineitem
        columns:
          - name: l_orderkey
            tests:
              - relationships:
                to: source('ptch', 'orders')
                field: o_orderkey
